# API

This folder is reserved for the backend API if and when the mailbox platform is split from the web app runtime.

Early development may keep route handlers close to the web app, but domain logic should remain portable so it can move here without rewriting the product.
