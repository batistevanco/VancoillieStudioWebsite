# Database Package

Database schema, migrations, and data access boundaries.

Initial target is Hostinger MySQL, but the data model must stay portable enough to move to a more scalable managed database setup later.

Sensitive data must be encrypted before storage where applicable.
